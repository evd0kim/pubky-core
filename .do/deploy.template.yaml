spec:
  name: digital-ocean-homeserver
  services:
    - name: web
      git:
        branch: client-refactoring-stage-1
        repo_clone_url: https://github.com/evd0kim/pubky-core.git
      envs:
        - key: DB_FOO_1
          value: "DB-password-testvalue"
          type: SECRET
        - key: EMAIL
          scope: RUN_TIME
        - key: MESSAGE
          value: "This is a greeting message"
